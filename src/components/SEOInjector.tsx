"use client";

// All analytics scripts are now injected server-side in app/layout.tsx.
// This component is kept as a stub so existing imports elsewhere don't break.
export default function SEOInjector() {
  return null;
}
