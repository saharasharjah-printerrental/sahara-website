// Moved to src/components/ui/Toast.tsx (it has no admin-specific
// dependency). Re-exported here so existing admin-page imports keep working.
export { Toast, useToast } from "@/components/ui/Toast";
